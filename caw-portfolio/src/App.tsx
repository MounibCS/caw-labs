import AnimusUI from "@/components/ac3-scene"

/* 
  Note: MobileWarning has been removed to allow mobile access as per requirements.
  Responsiveness is being addressed via CSS updates.
*/

export default function App() {
    return (
        <main className="app-main">
            <AnimusUI />
        </main>
    )
}
